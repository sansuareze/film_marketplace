<div class = "descriptioncontainer">
  <h2 class = "d-flex justify-content-center"><%= @equipment.title %></h2>
  <% if @equipment.photo.key %>

  <%= cl_image_tag @equipment.photo.key, height: 300, width: 325%>
  <% end %>
  <p>Description: <%= @equipment.description%></p>
  <p>Category: <%= @equipment.category %></p>
  <p>State: <%= @equipment.state %></p>
  <h4>€<%= @equipment.daily_price %>.00</h4>
  <%= button_to 'I Wanna Rent It!', new_equipment_booking_path(@equipment, @booking), method: :get, class: "btn-primary"%>
  <br>
  <%= link_to "Back to postings", equipment_index_path %>
</div>
